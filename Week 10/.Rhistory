demo()
smooth
print("Hello, World!")
print("Hello, World!");
open.connection(www.google.com)
print(2 + 3)
print(2 + 3.0)
print(2.1 + 3.0)
clear
clear()
open.connection(www.google.com)
open.connection(www.google.com)
print(2 + 3)
demo()
print("test")
data("groceries.csv")
install.packages("arules")
library(arules)
read.csv("groceries.csv")
setwd("~/Desktop/CS 450/Week 10")
read.csv("groceries.csv")
groceries <- read.csv("groceries.csv")
View(groceries)
groceries <- read.csv("groceries.csv", header = FALSE)
View(groceries)
groceries <- read.transactions("groceries.csv", header = FALSE, sep = ",")
groceries <- read.transactions("groceries.csv", sep = ",")
View(groceries)
View(groceries)
View(groceries)
summary(groceries)
inspect(groceries[1:5])
inspect(groceries[0:5])
inspect(groceries[0:0])
inspect(groceries[0:1])
inspect(groceries[1:1])
inspect(groceries[1:2])
inspect(groceries[1:20])
inspect(groceries[, 1:2])
inspect(groceries[1:5, 1])
inspect(groceries[1:5, 3])
inspect(groceries[1:5, 1:5])
itemFrequency(groceries[,1:3])
itemFrequency(groceries[1:20,1:3])
itemFrequency(groceries[1:2000,1:3])
itemFrequency(groceries[1:,1:3])
itemFrequency(groceries[,1:3])
itemFrequencyPlot(groceries, support = .2)
itemFrequencyPlot(groceries, support = .1)
itemFrequencyPlot(groceries, support = .05)
itemFrequencyPlot(groceries, support = .09)
itemFrequencyPlot(groceries, support = .1)
itemFrequencyPlot(groceries, support = .13)
groceries <- read.transactions("groceries.csv", topN = 10)
itemFrequencyPlot(groceries, topN = 20)
itemFrequencyPlot(groceries, topN = 10)
itemFrequencyPlot(groceries, topN = 5)
rules <- apriori(data = groceries, parameter = list(support = .5, confidence = .5, minlen = 1))
inspect(rules)
inspect(rules[1:5])
rules <- apriori(data = groceries, parameter = list(support = .5, confidence = .5, minlen = 1))
inspect(rules)
View(rules)
View(rules)
rules <- apriori(data = groceries, parameter = list(support = .5, confidence = .5, minlen = 1))
rules <- apriori(data = groceries, parameter = list(support = .001, confidence = .05, minlen = 1))
View(rules)
View(rules)
inspect(rules)
inspect(rules[1:5])
rules <- apriori(data = groceries, parameter = list(support = .001, confidence = .05, minlen = 2))
inspect(rules[1:5])
rules <- apriori(data = groceries, parameter = list(support = .01, confidence = .05, minlen = 2))
inspect(rules[1:5])
rules <- apriori(data = groceries, parameter = list(support = .1, confidence = .05, minlen = 2))
inspect(rules[1:5])
inspect(rules)
rules <- apriori(data = groceries, parameter = list(support = .01, confidence = .05, minlen = 2))
inspect(rules)
rules <- apriori(data = groceries, parameter = list(support = .015, confidence = .05, minlen = 2))
rules <- apriori(data = groceries, parameter = list(support = .02, confidence = .05, minlen = 2))
rules <- apriori(data = groceries, parameter = list(support = .02, confidence = .1, minlen = 2))
inspect(rules)
rules <- apriori(data = groceries, parameter = list(support = .1, confidence = .1, minlen = 2))
02
rules <- apriori(data = groceries, parameter = list(support = .02, confidence = .1, minlen = 2))
rules <- apriori(data = groceries, parameter = list(support = .05, confidence = .1, minlen = 2))
inspect(rules)
rules <- apriori(data = groceries, parameter = list(support = .05, confidence = .1, minlen = 1))
inspect(rules)
rules <- apriori(data = groceries, parameter = list(support = .05, confidence = .1, minlen = 2))
inspect(rules)
rules <- apriori(data = groceries, parameter = list(support = .006, confidence = .1, minlen = 2))
inspect(rules)
inspect(rules)
rules <- apriori(data = groceries, parameter = list(support = .006, confidence = .5, minlen = 2))
inspect(rules)
rules <- apriori(data = groceries, parameter = list(support = .006, confidence = .25, minlen = 2))
inspect(sort(rules, by = "support")[1:5])
rules <- apriori(data = groceries, parameter = list(support = .006, confidence = .5, minlen = 2))
rules <- apriori(data = groceries, parameter = list(support = .006, confidence = .5, minlen = 2))
inspect(rules)
inspect(sort(rules, by = "support")[1:5])
inspect(sort(rules, by = "confidence")[1:5])
inspect(sort(rules, by = "lift")[1:5])
print(inspect(sort(rules, by = "support")[1:5]))
inspect(sort(rules, by = "support")[1:5])
inspect(sort(rules, by = "confidence")[1:5])
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = .006, confidence = .3, minlen = 2))
inspect(sort(rules, by = "support")[1:5])
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (30 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "support")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (90 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "support")[1:5])
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (120 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (200 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (120 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
inspect(sort(rules, by = "confidence")[1:5])
inspect(sort(rules, by = "support")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (90 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (10 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (1 / length(groceries)), confidence = .3, minlen = 2))
rules <- apriori(data = groceries, parameter = list(support = (120 / length(groceries)), confidence = .3, minlen = 2))
rules <- apriori(data = groceries, parameter = list(support = (20 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (10 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (5 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
inspect(sort(rules, by = "confidence")[1:5])
inspect(sort(rules, by = "support")[1:5])
inspect(sort(rules, by = "confidence")[1:5])
inspect(sort(rules, by = "support")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (50 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "support")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (500 / length(groceries)), confidence = .3, minlen = 2))
inspect(sort(rules, by = "support")[1:5])
inspect(sort(rules, by = "support")[1:3])
rules <- apriori(data = groceries, parameter = list(support = (500 / length(groceries)), confidence = .03, minlen = 2))
inspect(sort(rules, by = "support")[1:3])
inspect(sort(rules, by = "support"))
rules <- apriori(data = groceries, parameter = list(support = (200 / length(groceries)), confidence = .003, minlen = 2))
inspect(sort(rules, by = "support"))
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (50 / length(groceries)), confidence = .25, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (45 / length(groceries)), confidence = .25, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (45 / length(groceries)), confidence = .003, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (40 / length(groceries)), confidence = .003, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
rules <- apriori(data = groceries, parameter = list(support = (400 / length(groceries)), confidence = .003, minlen = 2))
inspect(sort(rules, by = "support"))
rules <- apriori(data = groceries, parameter = list(support = (20 / length(groceries)), confidence = .003, minlen = 2))
inspect(sort(rules, by = "lift")[1:5])
